# Сервис сбора курсов валют

Профессиональный Python-сервис для автоматического сбора курсов валют с публичных API с хранением в PostgreSQL и продвинутой аналитикой.

## 🎯 Основные возможности

- **Автоматический сбор данных**: Запросы API каждые 5 минут
- **Надежное хранение**: PostgreSQL с внешними ключами  
- **SQL аналитика**: Продвинутые JOIN-запросы для анализа данных
- **Логирование ошибок**: Комплексная обработка ошибок с ротацией файлов
- **Docker готовность**: Production-готовое контейнеризованное развертывание
- **Модульная архитектура**: Чистая архитектура с разделением ответственности

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- PostgreSQL 12+
- Docker & Docker Compose (опционально)

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/mirash21/currency-api-service.git
cd currency-api-service

# Установить зависимости
pip install -r requirements.txt

# Настроить окружение
cp .env.example .env
# Отредактируйте .env с вашими настройками

# Запустить сервис
python currency_service.py
```

### Развертывание через Docker

```bash
# Сборка и запуск
make first-run

# Просмотр логов
make logs

# Запуск тестов
make test
```

## 📊 Структура базы данных

Две связанные таблицы с внешним ключом:

**Таблица requests**
```sql
CREATE TABLE requests (
    id SERIAL PRIMARY KEY,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    request_type VARCHAR(50) NOT NULL,
    status VARCHAR(20) NOT NULL,
    error_message TEXT
);
```

**Таблица responses** 
```sql
CREATE TABLE responses (
    id SERIAL PRIMARY KEY,
    request_id INTEGER REFERENCES requests(id) ON DELETE CASCADE,
    currency_code VARCHAR(3) NOT NULL,
    rate NUMERIC(16, 8) NOT NULL,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 🔍 SQL JOIN запросы

### История запросов с курсами валют
```sql
SELECT 
    r.id as request_id,
    r.timestamp as request_time,
    r.status,
    res.currency_code,
    res.rate
FROM requests r
LEFT JOIN responses res ON r.id = res.request_id
ORDER BY r.timestamp DESC;
```

### Агрегированная статистика
```sql  
SELECT 
    r.id,
    COUNT(res.id) as currency_count,
    STRING_AGG(res.currency_code || ': ' || res.rate::TEXT, ', ') as rates
FROM requests r
LEFT JOIN responses res ON r.id = res.request_id
GROUP BY r.id;
```

## 📁 Структура проекта

```
├── config.py              # Управление конфигурацией
├── logger.py             # Система логирования
├── database.py           # Операции с PostgreSQL  
├── api_client.py         # Клиент API
├── currency_service.py   # Основной сервис
├── demo_service.py       # Демонстрационный режим
├── test_service.py       # Тестовый набор
├── view_data.py          # Инспекция данных
├── run_sql_demo.py       # Демонстрации SQL
├── sql_queries.sql       # Библиотека SQL запросов
├── requirements.txt      # Зависимости
├── .env.example         # Шаблон конфигурации
├── Dockerfile           # Образ Docker
├── docker-compose.yml   # Оркестрация
├── Makefile             # Команды
└── README.md            # Документация
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
python test_service.py

# Тест подключения к базе данных
python -c "from database import db_manager; print('Подключено:', db_manager.connect())"

# Тест подключения к API  
python -c "from api_client import api_client; print('Статус API:', api_client.health_check())"
```

## 📈 Мониторинг

- Консольный вывод с детальной информацией
- Логирование ошибок в `error.log` с ротацией
- Записи базы данных всех запросов/ответов
- Встроенный дашборд статистики

## ⚙️ Конфигурация

Основные параметры в `.env`:
- `REQUEST_INTERVAL_MINUTES`: Частота сбора
- Настройки подключения к базе данных
- Конфигурация API ключа
- Параметры логирования

## 🛠 Используемые технологии

- **Python 3.11** с современными библиотеками
- **PostgreSQL 15** для надежного хранения
- **ExchangeRate-API** для данных курсов валют
- **Docker** для контейнеризации
- **Schedule** для планирования задач
- **Psycopg2** для подключения к базе данных

## 📄 Лицензия

MIT License - Открытое использование.